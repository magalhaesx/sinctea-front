# Copie para .env.local (ignorado pelo git) e ajuste.

# Endereco da API real. Deixe vazio para usar a implementacao simulada.
VITE_API_URL=

# Probabilidade de falha simulada no mock, de 0 a 1.
# 0.1 faz 10% das chamadas falharem; use para exercitar os estados de erro.
VITE_MOCK_FALHA=0
